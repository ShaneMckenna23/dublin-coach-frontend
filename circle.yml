version: 2
jobs:
  build:
    docker:
      - image: lakoo/node-gcloud-docker

    working_directory: ~/repo

    steps:
      - checkout

      - run: npm install bruce-one/node-zopfli

      - run: npm install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

      - run: npm run test

      - run: docker -v
